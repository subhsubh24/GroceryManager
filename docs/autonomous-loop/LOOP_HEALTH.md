# Loop health — is the LOOP getting better, or just busier?

The `QUALITY_SCORECARD` measures the **product**; this measures the **loop itself** (FACTORY_STANDARD §10b).

**Contract (read before editing):**
- **Update EVERY run, in the bookkeeping PR**, with REAL counts — `git log`/`gh` for merged/revert/readiness
  history + this run's own shipped/abandoned/verify/review/circuit-breaker tallies.
- **Honest only** — same anti-gaming rule as the business-case number. Never flatter a count; an ugly
  `churning`/`stuck` signal is the POINT (it triggers a harness proposal). `0`/`null` until real.
- **CLASSIFY every abandoned change** (`abandoned_reasons`) so the loop does NOT re-attempt the same dead-end
  — "don't repeat the failed path."
- **Dashboard-readable** — keep the fenced `LOOP_HEALTH` block valid, parseable YAML.
- **Refresh the `validation` sub-block every run** from `node scripts/check-self-validation.mjs --readiness`
  (`capabilities_total`/`active`/`unmet`/`unmet_unsurfaced`). Any **unmet** capability (one needing an owner
  secret the loop can't supply) MUST appear in BOTH `validation.unmet` here AND an urgent `OWNER_ACTION`
  `validation-capability-<service>` in `PENDING_OPS.md`; `unmet_unsurfaced` must stay empty.
- **Observability, NOT a ship gate** — this never blocks a merge or readiness; it informs.
- `signal` honest read: `churning` (high abandon/revert vs. shipped) or `stuck` (recurring failures / no
  convergence) → open ONE `loop: harness improvement proposal` issue (the META channel — the only way the
  loop's OWN rules improve, since it can't edit its routine / `.claude/`).
- `abandoned_reasons` enum is adapted to THIS stack (pnpm/Turborepo web + Expo mobile): `gate_tsc`
  (`pnpm -r run typecheck`), `gate_test` (`@gm/core` vitest), `gate_build` (`next build` + missing-export
  grep), `gate_mobile` (`apps/mobile` typecheck), `review_value`, `review_correctness`, `circuit_breaker`,
  `conflict`, `dead_end`, `blocked_owner`.

```yaml
LOOP_HEALTH:
  project: GroceryManager
  as_of: 2026-07-24 (run 93)
  enforced_in_ci: true           # lint + functional E2E journeys + the capabilities tripwire are REQUIRED checks on main, enforce_admins=true
  validation:                    # capability self-validation feed — refresh every run from `node scripts/check-self-validation.mjs --readiness`
    enforced_in_ci: true         # 'self-validation (capabilities tripwire)' is a required, enforce_admins status check
    capabilities_total: 8        # marketing-media-gen EXTENDED again run 68 (PR #544) — the launch-brief AUTHORING consumer (LAUNCH_CREATIVE_BRIEFS + auditLaunchBriefs + stageLaunchCreative) added under the same capability (still 8, not a new one); proven keyless via launch-briefs.test.ts (well-formed unique briefs, every item audit-clean, no-key all-'unavailable' degrade, all-ok via injected fake provider). Prior: batch staging consumer stageCreative run 63 (#515, staging.test.ts); adapter run 62 (#509).
    active: 8
    unmet: []                    # capabilities needing an OWNER SECRET not wired in CI (loop can't supply) — each MUST also be an urgent OWNER_ACTION 'validation-capability-<service>' in PENDING_OPS
    unmet_unsurfaced: []         # MUST stay empty — an unmet capability missing from PENDING_OPS or this list is invisible to the owner (a bug)
  last_run: 2026-07-24 (run 93)
  last_deep_audit: "2026-07-20 (run 89, standalone 4-Haiku lens sweep, due since run 86 [~24h]: security/RLS/Track-G · correctness/dead-code · design/a11y · artifact-freshness/monetization — 2 lenses NO-FINDINGS [security, monetization-reach], 2 a11y clears shipped, 1 correctness candidate rejected. SECURITY/RLS/Track-G: CLEAN — all tables through 0021 RLS+policy; ~41 routes rate-limit+zod+error-hygienic; constant-time auth verify #622 + login lockout; per-user LLM quota + demo per-IP/global ceiling; captcha; CSP/HSTS/nosniff; Stripe constructEvent + timing-safe RevenueCat/Gmail/cron webhooks; entitlements server-side; no committed secrets. CORRECTNESS: 1 candidate REJECTED — the public parse-receipt demo returns HTTP 200 with an error body [no items] on a zero-item successful extraction; the SOLE client demo-client.tsx:105 guards on !res.ok || !data.items so it handles it [no crash]; 200-for-zero-items is a defensible distinction from the 502 error path, and a 422 change is zero-behavior on the only caller = sub-bar. DESIGN/a11y: 2 clears — #630 cook-loop Cook/Cooked-it (btn-sm ~28px) + #631 Getting-Started/Install/See-Premium (btn-sm ~28px), both to base .btn + min-h-[44px] [the corrected #628 idiom, NOT btn-sm+min-h]; staples/page.tsx Set DEFERRED again [needs the adjacent number input raised + a browser visual check]. ARTIFACT/MONETIZATION: prices 499/3999/999/7999¢ byte-consistent across billing↔BUSINESS_CASE↔store↔privacy, SUMMARY valid YAML base $33,450/floor_met false; NO buildable non-reach lever [every pricing/tier/conversion/retention/margin lever built; ~$67K gap = ~2.7-3× downloads = owner-GTM #190]. Prior standalone run 86 [2026-07-19].)"
  _last_deep_audit_prev00: "2026-07-19 (run 86, standalone 6-Haiku lens sweep, due since run 83 [~24h]: security/RLS/Track-G · correctness/functional/uncaught-throws · Track-F coverage · design/a11y/taste · artifact-freshness/monetization · perf/mobile-parity — 3 real clears shipped, all other findings rejected/deferred. SECURITY/RLS/Track-G: 1 real G7 finding, both halves fixed → the COOK-LOGGING LLM-quota gap: mobile/cook (#618) AND web logCookedRecipe (#620) estimated macros via GeminiClient with NO checkLlmQuota gate — mobile had only a 30/min rate-limit, web had NONE — letting an authed user drain the paid Gemini API past the 10-free/100-premium daily G7 ceiling via FDC-unresolvable ingredients (every other LLM surface already gates). Fixed both: loadPreferenceSignals→isPremium→checkLlmQuota BEFORE constructing the LLM; degrade to keyless FDC-only macros on exhaustion, never blocking the core pantry-drawdown write; mirrors remix/plan. RLS/rest otherwise CLEAN — all 21 migrations' public tables RLS+policy (tenant_isolation / transitive-child / catalog_access→grocery_app / admin_access); ~42 routes auth+rate-limit+zod+error-hygienic; login lockout timing-safe no-enumeration; per-user LLM quota + demo per-IP/global ceiling; Turnstile fail-open-loud; Stripe constructEvent + timing-safe Gmail/RevenueCat/cron webhooks; CSP/HSTS/nosniff; entitlements server-side; no committed secrets; FDC 5s/Gemini 8s timeouts < serverless budget. CORRECTNESS/FUNCTIONAL: CLEAN — the scout's loadEnv/DB-outside-try findings on the gmail/stripe/revenuecat webhooks + gmail/digest/lifecycle crons REJECTED as the already-examined class (runs 57/74/77): a loadEnv throw = whole-app misconfig = correct fail-loud; a cron 500 → next scheduled run reconciles (cursor/idempotent, no data loss); per-item webhook sync already try-wrapped; benign Vercel retry, no real-outcome change. TEST-F: 1 shipped → #619 (units convert() unknown-code [index.ts:69] + zero-factor item-edge [:109] null paths → src/units branch 89.74%→95.12%; lines 124/141 left = bidirectional-graph dead guard + direction-symmetric water-density mirror = impossible-case padding, correctly skipped). DESIGN/a11y: CLEAN — manage-subscription `←` entity is within the established inline-text-arrow convention (NOT the standalone-glyph class #491 fixed; run-59/72 lesson); wrapped shareText emoji is idiomatic social-share copy, not a UI icon — both REJECTED. PERF/MOBILE: the vision/persist confirmations + capture/add trigram sequential-append N+1 candidates REJECTED as the ingest N+1 class prior audits (38/41) held sub-bar (LLM-bound / correctness-sensitive per-(user,item) ledger invariant / low-frequency handful-of-items). ARTIFACT/MONETIZATION: CLEAN — prices 499/3999/999/7999¢ byte-consistent across billing↔BUSINESS_CASE↔store↔.env; SUMMARY valid YAML base $33,450/floor_met false; every household/Family surface gated behind householdsEnabled(); NO buildable non-reach lever (every pricing/tier/conversion/retention/margin lever built; the $67K gap to the floor is a ~2.7-3× download multiplier = owner GTM #190). Prior standalone run 83 (2026-07-18).)"
  _last_deep_audit_prev0: "2026-07-18 (run 83, standalone 6-Haiku lens sweep, due since run 77 [runs 78-82 were scout sweeps]: security/RLS/Track-G · correctness/functional/uncaught-throws · Track-F coverage · design/a11y/taste · artifact-freshness/monetization · perf/mobile-parity — 2 lenses CLEAN [security, correctness], 3 real clears shipped, 1 finding rejected, 4 deferred. SECURITY/RLS/Track-G: CLEAN — all 21 migrations' public tables RLS+policy; ~42 routes auth+rate-limit+zod+error-hygienic; login lockout timing-safe no-enumeration; per-user LLM quota + demo per-IP/global spend ceiling; Turnstile fail-open-loud; Stripe constructEvent + timing-safe Gmail/RevenueCat/cron webhooks; CSP/HSTS/nosniff; entitlements server-side; no committed secrets. CORRECTNESS/FUNCTIONAL: CLEAN — all actions/routes try/catch→degrade; LLM/external withTimeout<budget; ledger invariant + trial-once held; no uncaught throws/TODO debt. TEST-F: 1 shipped → #607 (cook.ts:60 scaleMeasure decimal fallback → 100% line); 4 deferred as marginal/redundant. DESIGN/a11y: 1 REJECTED — recipe images alt=\"\"→alt={title} across 9 pages is an ANTI-PATTERN (title is adjacent/overlaid text → empty alt correct; the recurring alt='' Haiku trap). ARTIFACT/MONETIZATION: 1 shipped → #606 (.env.example billing section + OPERATIONS.md STRIPE_PRICE_FAMILY/REVENUECAT_WEBHOOK_AUTH); prices 499/3999/999/7999¢ + SUMMARY (base $33,450, floor_met false) re-confirmed; REACH-GATED (#190). PERF/MOBILE: 1 shipped → #605 (mobile paywall filtered gmail_import — no native Gmail surface; a REVERSAL of run 80's deferral, kept on the merits, process slipped). Prior folded run 77 (2026-07-16).)"
  _last_deep_audit_prev: "2026-07-15 (run 74, folded 5-Haiku lens sweep at the ~24h mark since the run-72 folded audit: correctness/dead-code+functional · security/RLS/Track-G+abuse · design/a11y/taste · artifact-freshness+monetization/business-case · test-coverage+perf+mobile — 4 of 5 lenses CLEAN, 1 real Track-F coverage finding shipped (#565). CORRECTNESS/FUNCTIONAL: CLEAN — external/LLM calls withTimeout/AbortSignal < serverless budget, keepAlive .catch→waitUntil, signup unique-violation race caught + surfaced, TOKEN_ENC_KEY loud-in-prod, webhooks fail-closed, ledger invariant held, no uncaught throws/TODO debt on live paths. SECURITY/RLS/Track-G: CLEAN — all public tables through 0021 RLS+policy (incl. referral_credits/lifecycle_email_sends/waitlist_invites, tenant_isolation); ~41 routes rate-limited + zod-validated + error-hygienic; login lockout (10→15min, timing-safe, no username enumeration); per-user LLM quota + demo per-IP(3/min)/global(500/day) spend ceiling; Turnstile fail-open-dev/loud-in-prod; Stripe constructEvent + timing-safe RevenueCat/Gmail/cron webhooks (fail-closed without metadata.userId, tiers default to lowest on misconfig); CSP/HSTS/X-Frame/nosniff/Permissions-Policy; entitlements server-side; no committed secrets. In-memory rate-limit/quota Maps are per-instance (Redis upgrade tracked in PENDING_OPS; mitigated pre-launch by owner site-gate) — not a code bug. DESIGN/a11y/TASTE: CLEAN — onboarding/paywall/home/signup/demo + mobile swept; lucide/Ionicons registries only, no generated surfaces, ≥44px targets, focus rings, humanize/titleCase, real DB-derived values (no placeholder). ARTIFACT/MONETIZATION: CLEAN — prices 499/3999/999/7999¢ byte-consistent across billing↔BUSINESS_CASE SUMMARY (base $33,450, floor_met false)↔store/ASO copy; grocerymanager.app canonical in code AND docs; privacy discloses IAP; PREMIUM_FEATURES↔ASO aligned. TEST-COVERAGE/PERF/MOBILE: 1 shipped → #565 (predict.ts recommendQty money-path branches — needed===0 par-met short-circuit + no-packageQty raw-shortfall path — were genuinely uncovered on origin/main at 90.9% branch/lines 113-114; added 2 regression guards via predictReorder + folded a null-par assertion inline → 100% line/fully branch-covered). Reviewer B rejected a 3rd duplicate test (0 coverage delta vs the existing supplements test) → dropped, folded inline. Mobile 'cancelled-flag → setState-on-unmounted' candidates REJECTED — React 19.2.7 removed that warning (React 18, 2022); late setState is a harmless no-op, no real harm (churn). Middleware perf gap (#320) still DEFERRED (loop-untouchable .github/ half; load-bearing auth refactor). MONETIZATION: REACH-GATED RE-CONFIRMED (base ≈ $33K < $100K = owner-GTM #190; no buildable floor-mover). Prior folded run 72 (2026-07-14).)"
  _last_deep_audit_prev2: "2026-07-14 (run 72, folded 5-Haiku lens sweep at the ~24h mark since the run-70 standalone: security/abuse+RLS/Track-G · design/a11y/taste · correctness/dead-code+functional · artifact-freshness+business-case · test-coverage+performance — ALL 5 LENSES CLEAN except ONE real artifact-drift finding shipped (#560) + the standing middleware perf gap (deferred). SECURITY/ABUSE/RLS: CLEAN — all public tables RLS+policy (tenant_isolation / catalog_access→grocery_app / admin_access); ~41 routes rate-limited + zod-validated + error-hygienic; login lockout (10→15min, timing-safe); per-user LLM quota (10 free/100 premium/day) + demo per-IP(3/min)/global daily spend ceiling; Turnstile fail-open-dev/closed-prod; Stripe constructEvent / Gmail / RevenueCat / cron webhooks signature/timing-safe; CSP/HSTS/X-Frame/nosniff/Permissions-Policy; entitlements server-side. Scout's lone CORS 'finding' (add Access-Control-Allow-Origin) REJECTED — would WEAKEN security (open cross-origin), no bug manifests (mobile native fetch, web same-origin); the run-35 rejected regression. DESIGN/a11y/TASTE: CLEAN — demo/onboarding/paywall/home/signup + mobile swept; lucide/Ionicons registries only, no generated surfaces, focus rings/labels/≥44px. Scout's raw-'→'-in-text-links finding REJECTED (inline → text-link arrows are an established deliberate convention, run-59 lesson; registry rule targets standalone ★/▾, not inline affordance arrows). CORRECTNESS/FUNCTIONAL: CLEAN — auth() wrapped, LLM/external calls withTimeout/AbortSignal, webhooks fail-closed, ledger invariant + depletion math tested, no uncaught throws/TODO debt on live paths. Scout's consume.ts usedUnmeasured 'mislabel' REJECTED as sub-bar — that count is NEVER surfaced (cook-actions.ts + mobile cook route return only decremented), zero user-visible/downstream effect (churn). ARTIFACT-FRESHNESS: 1 real drift shipped → #560 (OPERATIONS.md:55 listed NEXT_PUBLIC_VAPID_PUBLIC_KEY but app reads bare VAPID_PUBLIC_KEY everywhere — env.ts:58, push-send.ts, profile/digest via loadEnv()+prop-drill to client PushToggle, no client process.env.NEXT_PUBLIC_* read; operator-following-doc would silently disable web push in prod). BUSINESS_CASE prices (499/3999/999/7999¢) + SUMMARY YAML (base $33,450, floor_met false), capabilities.json files (11/11), ACCEPTANCE_AUDIT paths, privacy disclosures — all re-confirmed consistent. TEST-COVERAGE/PERF: CLEAN except the standing middleware gap. Exported branching core paths tested; hot paths batch reads (Promise.all), no N+1. Middleware (#320) DEFERRED — sole below-A perf dim (edge middleware.ts pulls next-auth/jose ~280KB raw onto ~every request); both fixes (narrow matcher / edge-JWT decode) are load-bearing AUTH refactors (narrowing risks exposing unprotected routes; edge-JWT needs crypto testing under edge-runtime) — wrong blast radius for unattended auto-merge, and perf is non-ship-critical at its ≥B target (the #320 CI perf-budget-gate half lives under .github/, loop-untouchable). MONETIZATION: REACH-GATED RE-CONFIRMED (base ≈ $33K < $100K = owner-GTM #190; no buildable floor-mover). Prior standalone run 70 (2026-07-13).)"
  _last_deep_audit_prev3: "2026-07-13 (run 70, folded 5-Haiku lens sweep at the ~24h mark since the run-66 standalone: newest-code correctness+coverage · security/abuse+RLS/Track-G · design+a11y+taste · artifact-freshness · monetization+business-case — ALL 5 LENSES CLEAN, 0 findings shipped (converged run). CORRECTNESS: the sole concrete candidate (client.ts:444 runChatWithTools tool-combine fallback tagging isRetry:false for Margin telemetry) was VERIFIED A FALSE POSITIVE — timedGenerate calls recordLlmCall AFTER `await withTimeout(call())` (client.ts:202-203), so the 400-throwing first call is NEVER metered; the fallback is therefore the FIRST/only metered call for step 1 and isRetry:false correctly marks it the primary attempt (the proposed isRetry:true fix would CORRUPT telemetry by emitting a retry with no primary). SECURITY/ABUSE/RLS: CLEAN — all 22 migrations' public tables RLS+policy; ~41 routes rate-limited + zod-validated + error-hygienic; login lockout (10→15min) + timing-safe compare; per-user LLM quota (10 free/100 premium/day) + demo per-IP(5/day)/global(500/day) spend ceiling; Turnstile fail-open-but-loud; Stripe constructEvent / Gmail / RevenueCat / cron webhooks signature/timing-safe; CSP/HSTS/X-Frame/nosniff/Permissions-Policy set; entitlements server-side; Margin telemetry emits metadata-only (no PII/secrets); secrets env-only. DESIGN/a11y/TASTE: CLEAN — demo/onboarding/paywall/home/signup swept; no generated-looking surface fails THE DESIGNER QUESTION; labeled interactive elements, focus rings, aria-live; the cook-mode Check conveys real pantry info (correctly NOT aria-hidden). ARTIFACT FRESHNESS: CONSISTENT — README layout, OPERATIONS/.env.example env roles (AUTH_SECRET/NEXTAUTH_SECRET post-#543), BUSINESS_CASE prices (499/3999/999/7999¢) + SUMMARY YAML (base $33,450, floor_met false), ACCEPTANCE_AUDIT paths, capabilities.json referenced files — all match code. MONETIZATION: REACH-GATED RE-CONFIRMED (base ≈ $33K < $100K = owner-GTM #190; every buildable pricing/tier/conversion/retention lever built; the scout's 4 'levers' are speculative pricing/experiment changes with no pre-launch signal, owner-flag-gated Family, or non-floor-movers closing ~20% of the gap — no buildable floor-mover). Prior standalone run 66 (2026-07-12).)"
  _last_deep_audit_prev4: "2026-07-11 (run 63, standalone 4-Haiku lens sweep, due since run 59 / ~4 runs: security/abuse+RLS · correctness/dead-code · test/eval coverage · design+a11y+artifact-freshness — 3 of 4 lenses CLEAN, 1 real correctness finding shipped. SECURITY/ABUSE/RLS: CLEAN — all 22 migrations' public tables RLS+policy (tenant_isolation / catalog_access / admin_access); all 41 routes rate-limited + zod-validated + error-hygienic; login lockout (10→15min); per-user LLM quota (10 free / 100 premium /day); Turnstile captcha fail-closed in prod; Stripe (constructEvent) / Gmail / RevenueCat webhooks signature/timing-safe; CSP/HSTS/X-Frame/X-Content-Type/Permissions-Policy set; entitlements server-side only; no hardcoded secrets. CORRECTNESS: 1 real finding → #514 — three growth admin routes (snapshot/analytics/email) called a bare `await auth()` OUTSIDE their try/catch; `auth()` THROWS on an undecryptable cookie (post-AUTH_SECRET rotation) → uncaught 500 not the intended 403; fixed by swapping to the non-throwing `currentSession()` helper. Reviewer B named a 4th raw-`auth()` site (`session-actions.ts:13`, LaunchGuard's forceSignOutAction) — fixed same-run as #517. TEST/EVAL COVERAGE: CLEAN — ~977 tests; all money/security/core exported paths + error/degrade branches covered; 'untested' files are pure barrel re-exports. DESIGN/a11y/ARTIFACTS: CLEAN — no generated-looking surface; inputs labeled; lucide-only; BUSINESS_CASE prices/ARR byte-consistent with billing (Premium 499/3999/999¢, base $33,450, floor_met false); privacy disclosures match data flows; README/LAUNCH accurate. Reach-gated RE-CONFIRMED (base ≈ $33K < $100K = owner-GTM #190; no buildable floor-mover). Prior standalone run 59 (~4 runs ago).)"
  _last_deep_audit_prev5: "2026-07-10 (run 59, standalone 5-Haiku lens sweep, due since run 57 / ~4 runs: security/RLS+Track-G, correctness/dead-code+functional-reality, test-coverage+performance, design/a11y/taste, artifacts+business-case — 2 real findings shipped + 1 reviewer-surfaced follow-on, everything else CLEAN. SECURITY/RLS/Track-G: CLEAN — all public tables through 0021 RLS+policy, rate-limits/captcha/HMAC/webhook-auth (Stripe constructEvent + timing-safe RevenueCat/Gmail/cron/site-gate)/headers/CORS/per-user LLM quota all present, AES-256-GCM at rest, no new hole since 0021. CORRECTNESS/FUNCTIONAL: CLEAN — all server actions + routes try/catch→degrade, run-57 pantry wraps hold, LLM withTimeout(8s), DATABASE_URL fails-loud, no uncaught throws/dead-ends on critical paths. TEST/PERF: CLEAN above marginal — 912 tests / coverage 87/88/91 > thresholds, recent bug-fixes all landed with regression tests; ingest/capture N+1 + sequential-insert candidates STILL rejected (LLM-bound, <2% on a correctness-sensitive path — run-38/41 verdict); rankRecipes batchCook weighting untested but dormant/never-applied (not a bug). DESIGN: 1 real customer-facing shipped (#491 — meal-generator.tsx:97 literal ▾ disclosure glyph → <ChevronDown> registry icon, the lone glyph left after cook-mode #479/#486; inline → text-link arrows correctly NOT flagged — established convention). ARTIFACTS: 1 real store-risk shipped (#492 — privacy-disclosures.md §1.4 falsely declared 'NO in-app purchases or subscriptions' while RevenueCat mobile + Stripe web subscriptions are wired at 499/3999/999/7999 cents; corrected §1.4→YES + Play data-map row + Apple checklist, §1.5 payment-credentials stays NO; prices byte-verified vs packages/core/src/billing) + 1 reviewer-surfaced follow-on (#493 — @gm/core/billing header comment 'SCAFFOLD ONLY (no real payments yet)' contradicted wired billing → corrected, comment-only). BUSINESS_CASE prices/ARR/floor byte-consistent (base $33,450, floor_met false). Reach-gated RE-CONFIRMED (base ≈ $33K < $100K = owner-GTM #190; no buildable floor-mover in the sweep). Prior standalone run 57 (~4 runs ago).)"
  this_run:
    changes_shipped: 3           # run 93 — DEEP AUDIT not due (run 92 ran a full 7-lens sweep <24h prior). Focused 3-Haiku scout (a11y/design-taste · mobile functional-reality · core Track-F coverage) advancing the run-92 follow-ups. 3 file-disjoint clears shipped, 1 abandoned (#645). #642 (fix/mobile pantry names — the native Pantry screen rendered raw lowercase canonical slugs [`item.name`, "organic hass avocados"] while web title-cases; fixed in the pure tested seam toMobilePantryItems [server-side mapper], +3 guard cases; 2/2 first-pass). #644 (a11y/recipes — the recipe-card cook-loop row paired a min-h-[44px] "Cook →" with two text-xs sub-44px inline controls [Remix link ~28px + "+ Add N missing to list" submit]; both raised to min-h-[44px] px-1; 2/2 first-pass). #643 (refactor/format — extracted titleCase into @gm/core/format as the SINGLE source of truth; the pantry DTO mapper reuses it [drops its inline copy], the three remaining raw-name native screens [list/use-it-up/spend] title-case at render via the @gm/core/* alias, AND apps/web/app/lib/format.ts re-exports it dropping its own copy so web+mobile can't drift; +6-case unit test; 2 rework cycles [B: v1 mobile-local copy = avoidable dup → use @gm/core; v2 core copy left web copy un-unified → also re-export in web] → 2/2). Abandoned #645 (a11y/cook "Remix this recipe" 44px) — Reviewer A correctly showed min-h+items-center doubles the visible gap & shifts CookMode down; the correct fix is the negative-margin idiom (pantry:408/save-button:45), layout-sensitive + unverifiable headless → abandoned, re-queued as a follow-up. LESSON (see LOOP_MEMORY): a workspace-excluded native app CAN still import pure @gm/core logic via the tsconfig @gm/core/* alias — reuse it rather than hand-copy a display rule (drift risk); and a "single source of truth" claim is only true once ALL copies (incl. the web lib's) route through the shared module. monetization NO buildable non-reach lever (reach-gated #190 re-confirmed, ~11th consecutive run). Did NOT open 'ready' (floor gap: base $33K < $100K reach-gated #190; Confidence UNCHECKED). validation 8/8, 0 unmet; ship gate MET (A).
    _prev_run_shipped_92: 4      # run 92 — DEEP AUDIT (7-Haiku lens sweep, DUE since run 89 ~4 days/>24h) + 4 file-disjoint clears; 8/8 first-pass Sonnet approve, 0 abandons, 0 review cycles, 0 circuit-breaks. FLAGSHIP #638 (fix/mobile pantry contract — CRITICAL dead-on-arrival: the native Pantry screen read {id,quantity,unit,daysUntilExpiry} but /api/mobile/pantry returns getPantryView rows {canonicalItemId,name,baseQtyOnHand,status,estimatedRunOutAt} → blank quantities, undefined React keys, "undefinedd left"; reconciled the shapes in ONE pure tested place, toMobilePantryItems in @gm/core/pantry [NaN-safe "N on hand" mirroring web, "Expires"→"Runs out" honesty relabel since the field is a consumption prediction not a spoilage date], 8-case keyless guard mobile-view.test.ts). #637 (fix/mobile onboarding — step-3 finish awaited apiFetch then router.replace("/") UNCONDITIONALLY; apiFetch resolves on 5xx so a failed finish left onboarding unmarked server-side → re-onboard loop next launch; gated the redirect on res.ok, the idiom the other mobile screens use). #639 (perf/pantry — reprojectStock's 2 independent reads [ledger events + item shelf-life] dispatched together via Promise.all on the hottest write path [per purchase line, per cook ingredient]; Reviewer A traced porsager/postgres tx-queue = FIFO-serialized/safe, established in ask/actions.ts; MAKER pre-PR accuracy self-correction: "removes JS-side await stall" not "saves a wire round-trip"). #640 (a11y/ask — /ask starter chips [interactive buttons on .pill-brand ~24px] → min-h-[44px] px-4, shared .pill untouched so the 7 display badges stay compact). LENSES (7): mobile/functional-reality = CRITICAL+1 (both shipped #638/#637); correctness (ingest `!` "critical ledger corruption" = FALSE POSITIVE — Drizzle .returning() on a single-row insert yields one row or throws, never empty); perf (log-cook/ingest N+1 DEFERRED — order-dependent reproject / risky refactor); security/RLS/Track-G CLEAN (RLS on all public tables, constant-time auth, captcha, CSP/HSTS/nosniff, Stripe constructEvent + timing-safe webhooks, server-side entitlements; multi-instance in-memory ceiling OWNER-GATED, already tracked LAUNCH Step 10; CORS "missing ACAO" = the secure same-origin default not a hole; Stripe non-UUID userId already fails safe); design/a11y (/digest pill-brand LINK page.tsx:291 noted as next candidate); artifact-freshness (prices/features/models match code; privacy-disclosures owner-email placeholder already tracked in ACCEPTANCE_AUDIT); coverage (mobile JSON-boundary contract-test seam = follow-up, the #638 pure mappers are the seam). monetization NO buildable non-reach lever (reach-gated #190 re-confirmed ~10th consecutive run). Did NOT open 'ready' (floor gap: base $33K < $100K reach-gated #190; Confidence UNCHECKED). validation 8/8, 0 unmet; ship gate MET (A).
    _prev_run_shipped_91: 1      # run 91 — targeted INDEPENDENT re-verification (4-Haiku adversarial: security/Track-G · monetization non-reach lever · correctness/core-loop · design/a11y/artifact) since the run-89 deep audit (DEEP AUDIT not due, ~2 runs prior) + 1 file-disjoint correctness clear + 1 LAUNCH handoff-completeness fix (this PR). 3/3 Sonnet approve (1 review cycle on #634 — Reviewer B REQUEST_CHANGES on OVERSTATED reachability framing [claimed all 3 cook surfaces; only the mobile API route + a crafted web-action POST reach fractional servings — the web form min=1/step=1 & the mobile stepper are integer-clamped]; corrected framing → APPROVE). #634 (fix/nutrition — estimateMealMacros clamped 0<servings<1 up to 1× via Math.max(1,servings) while the pantry drawdown scaled fractionally [planConsumption], so a half-batch cook logged full-recipe macros; changed to `Number.isFinite&&>0?servings:1` matching logCook/planConsumption, also closes a latent Math.max(1,NaN)=NaN propagation bug; +2 regression tests). LAUNCH.md — added Step 10 (provision an Upstash-Redis shared rate-limit/quota/demo-spend store for multi-instance safety + set PROVIDER SPEND CAPS + alerts before public traffic), surfacing the security scout's top finding [PENDING_OPS llm-quota-redis-upgrade] into the owner checklist. LENSES: security/Track-G 1 finding = the multi-instance in-memory-Map ceiling (real at scale but OWNER-GATED infra, already tracked; the degrade path is CI-validated, the real fix needs owner Upstash provisioning + is un-provable keyless → correctly deferred, now surfaced in LAUNCH); monetization NO buildable non-reach lever (reach-gated re-confirmed #190); design/a11y 2 candidates REJECTED (wrapped shareText emoji = idiomatic social-share copy not a UI icon; bottom-nav 36px icon-span sits inside a ~89×61px full-link tap target). Did NOT open 'ready' (floor gap stands: base $33K < $100K, reach-gated #190; Confidence UNCHECKED). validation 8/8, 0 unmet; ship gate MET (A).
    _prev_run_shipped_90: 1      # run 90 — targeted 4-Haiku independent re-verification (DEEP AUDIT not due, run 89 same-cadence) + 1 file-disjoint core-loop correctness/UX-honesty clear. #633 (fix/ingest — the receipt→pantry success copy led with linesIngested [every parsed line, incl. low-confidence ones parked in the Review inbox] instead of items ACTUALLY added to the pantry; added addedToPantry to IngestResult incremented at the appendLedgerAndReproject write site [ledger-truth, not display arithmetic], threaded through the Gmail rollup; both surfaces now lead with items added, review parkers reported separately; folded a pre-existing need${plural(n)} verb-agreement bug). Did NOT open 'ready' (reach-gated #190; Confidence UNCHECKED). validation 8/8, 0 unmet; ship gate MET (A).
    _prev_run_shipped_89: 2      # run 89 — standalone 4-Haiku DEEP AUDIT (due since run 86 ~24h) + 2 file-disjoint a11y clears with the CORRECTED 44px idiom; 4/4 Sonnet approve (1 review cycle on #630 — Reviewer B REQUEST_CHANGES on the run-88 btn-sm+min-h anti-pattern [a 44px box on a 12px text-xs label]; reworked to base .btn+min-h-[44px] per the #628 idiom → 2/2). #630 (fix/a11y — cook-loop Cook→ [recipes+use-it-up] + the shared Cooked-it button, btn-sm ~28px → base .btn+min-h-[44px]; the shared default className carries both call sites). #631 (fix/a11y — Getting-Started checklist links + PWA Install button + Gmail See-Premium CTA, btn-sm ~28px → base .btn+min-h-[44px]; install-prompt dismiss X already 44px). DEEP AUDIT: security/RLS/Track-G + monetization-reach lenses NO-FINDINGS (reach-gated re-confirmed, no buildable non-reach lever); correctness 1 candidate REJECTED (parse-receipt HTTP-200-with-error on zero items — sole client demo-client.tsx:105 guards !res.ok||!data.items, no crash, 422 change zero-behavior); staples/page.tsx Set DEFERRED (adjacent number input + browser visual check needed). PROCESS LESSON: read the latest LOOP_MEMORY design-idiom lesson before choosing an idiom — the latest MERGED commit (#627) can BE the anti-pattern the same run's lesson corrects; prefer the exemplar the LESSON endorses (#628), not the latest-merged. Did NOT open 'ready' (floor gap stands: base $33K < $100K, reach-gated #190; Confidence UNCHECKED). validation 8/8, 0 unmet; ship gate MET (A).
    _prev_run_shipped_88: 4      # run 88 — 5-Haiku scout sweep (folded deep-audit-equivalent; deep audit NOT due — run 86 same day) + 4 file-disjoint clears; 8/8 Sonnet approve (review-list #628 took 1 review cycle — Reviewer B REQUEST_CHANGES on the btn-sm+min-h-[44px] glued combo [a 44px box around a 12px text-xs label = off-precedent tall pill]; revised to base .btn + min-h-[44px] per the onboarding-flow CTA precedent → 2/2). #625 (docs/marketing — dropped the dark FEATURE_HOUSEHOLDS "household sharing" claims from 3 STAGED launch artifacts [EMAIL_LIFECYCLE trial/expiry emails, PRESS_KIT, CONTENT_DRAFTS], substituting real reachable perks [remix/Discover/spend]; store-honesty/Apple-2.3.1, extends the runs 78-82 dark-feature sweep onto MARKETING copy — the paywalls + store metadata already omit household). #626 (test/units — MASS→VOLUME water-density heuristic direction [index.ts:141], reachable UNFILTERED via ingest.ts [logCook discards heuristic results]; branch 95.12%→97.36%, Track-F). #627 (fix/a11y — pantry "Still good?" resolution chips btn-sm ~28px→44px). #628 (fix/a11y — review-queue bulk Add-to-pantry + dismiss controls ~28px→44px, base .btn+min-h). SECURITY + CORRECTNESS lenses CLEAN (both scouts NO findings). Did NOT open 'ready' (floor gap stands: base $33K < $100K, reach-gated #190; Confidence UNCHECKED). validation 8/8, 0 unmet; ship gate MET (A). FOLLOW-UP noted: pantry #627 shipped btn-sm+min-h-[44px] (the pattern #628's reviewer later flagged); pantry resolution chips + staples/page.tsx:169 Set + the dosage number input are candidates for the proportionate .chip-tap idiom in a future design-polish pass.
    _prev_run_shipped_86: 3      # run 86 — DEEP AUDIT (6-Haiku lens sweep, due since run 83 ~24h) + 3 file-disjoint clears; 6/6 Sonnet approve (Reviewer A on #620 first REQUEST_CHANGES on a STALE premise — it read main's still-unfixed mobile/cook route — then revised to APPROVE once shown #618 fixes that exact file the same run, file-disjoint; maker≠checker resolved via a factual correction, not an override). #618 (fix/mobile) + #620 (fix/web): Track-G G7 — mobile/cook AND web logCookedRecipe estimated macros via GeminiClient with NO checkLlmQuota gate (mobile had only 30/min rate-limit, web had NONE), draining the paid Gemini API past the 10-free/100-premium daily spend ceiling via FDC-unresolvable ingredients; gated both (loadPreferenceSignals→isPremium→checkLlmQuota BEFORE the LLM, degrade to keyless macros on exhaustion, never blocking the core cook write). #619 (test/units — convert() unknown-code + zero-factor-edge null coverage → src/units branch 89.74%→95.12%, Track-F). REJECTED (with evidence): webhook/cron loadEnv/DB-outside-try (already-examined benign-retry class, runs 57/74/77); ingest N+1 in vision/persist + capture/add (runs 38/41 sub-bar); manage-subscription `←` entity (inline-text-arrow convention, run-59/72); wrapped shareText emoji (idiomatic social copy). Did NOT open 'ready' (floor gap stands: base $33K < $100K, reach-gated #190; Confidence UNCHECKED). validation 8/8, 0 unmet; ship gate MET (A).
    _prev_run_shipped_0: 0       # run 85 — QUIET CONVERGENT run (0 code changes). INDEPENDENT 4-Haiku adversarial re-verification on commit ca9f25b (maker≠checker, fresh run). Commits since run 84 were ALL doctrine/CI/bookkeeping — zero product source. 4 fresh scouts (store-honesty · security/RLS/Track-G · design+a11y+Track-F · monetization non-reach lever) ALL NO NEW FINDINGS, corroborating own maker checks (dark-feature grep CLEAN; SUMMARY valid YAML base $33,450/floor_met false; ship gate MET at A; migration 0021 intact). Shipped nothing. Did NOT open 'ready' (reach-gated #190; Confidence UNCHECKED). validation 8/8, 0 unmet.
    _prev_run_shipped: 0         # run 84 — QUIET CONVERGENT run (0 code changes). DEEP AUDIT not due (run 83 same-day). Two lean adversarial Haiku scouts: (1) no GENUINELY-NEW value-bar-clearing change; (2) NO buildable non-reach revenue lever — every pricing/tier/conversion/retention/margin lever BUILT, the $67K gap is demand-driven (owner GTM). Shipping marginal work would be padding (FORBIDDEN). Did NOT open 'ready' (reach-gated #190; Confidence UNCHECKED). validation 8/8, 0 unmet.
    _prev_run_shipped_2: 2       # run 82 — 5-Haiku scout sweep (folded deep-audit-equivalent: Track-F coverage · security/RLS/Track-G · design/a11y+living-artifact · correctness/mobile-parity · monetization/store-honesty, read-only over the whole repo; run 77 folded a standalone ~24-30h earlier) + 2 file-disjoint clears; 3 of 5 lenses NO-FINDINGS (security/RLS/Track-G — CLEAN: all 13 tenant + 3 child + 6 catalog + admin/waitlist tables through 0021 RLS+policy, ~41 routes auth/rate-limit/zod/error-hygienic, login lockout timing-safe no-enumeration, per-user LLM quota + demo per-IP/global ceiling, Turnstile fail-open-loud, Stripe constructEvent + timing-safe RevenueCat/Gmail/cron webhooks, CSP/HSTS/nosniff, entitlements server-side, no committed secrets; Track-F coverage — NO CLEAN GAP after verifying sibling+barrel tests across recipe/reorder/pantry/units/nutrition/personalization/experiments/ingestion/capture/billing/security; correctness/mobile-parity — CLEAN, all critical paths try/catch + LLM withTimeout, no dead code, mobile paywall perks all reachable [household dark-filtered, gmail_import deliberate-deferred]). #602 (fix/billing — manage-subscription/page.tsx [reachable free-user page linked from /profile] advertised "household sharing" in its upgrade CTA AND rendered the Family [premium_family] plan card UNCONDITIONALLY while FEATURE_HOUSEHOLDS is off, the lone remaining surface not gated after runs 78-81 did /help, mobile paywall, landing, /upgrade; mirrored the /upgrade gate — imported householdsEnabled from @gm/db, computed householdsLive, dropped the perk copy + filtered premium_family from the plans grid when dark; Apple 2.3.1 / accurate-listing). #601 (fix/a11y — install-prompt.tsx PWA install-banner dismiss button was btn-ghost btn-sm [~28px tall < the repo's 44px touch-target bar]; switched to the established banner-dismiss idiom flex h-11 w-11 items-center justify-center [the #584 Gmail-banner / theme-toggle precedent] with neutral hover:bg-ink-50, aria-hidden on the icon; same fix class as #584 in a second component). Both merged through required CI; 4/4 Sonnet approve first-pass.
    changes_abandoned: 1         # run 93 — #645 (a11y/cook "Remix this recipe" 44px). run 92 — 0.
    abandoned_reasons: ["review_correctness"]  # run 93 — #645: Reviewer A REQUEST_CHANGES (correct) — the naive min-h-[44px]+items-center adds ~12px centering space above/below the text so the mt-2→mt-1 nudge doubled the visible gap and pushed the CookMode section down; the correct fix is the repo's negative-margin offset idiom (pantry/page.tsx:408, save-button.tsx:45), which is layout-sensitive and can't be visually verified in this headless env. Abandoned rather than ship an unverified layout tweak on a design-bar repo; re-queued as a follow-up (with the same-screen back-link/nav-link controls Reviewer B flagged). NOT a circuit-break (single cycle, clean tree, judgment abandon).
    verify_cycle_failures: 0     # run 93 — 0 gate failures across the 3 shipped clears. #642 core: full @gm/core suite green (1061 pass +27 skip), typecheck 6/6, prod web build exit 0 no missing-export. #644 web: typecheck + prod build green. #643 (rework to shared @gm/core/format + web re-export): @gm/core suite green (1067 pass, +6 from the new format.test.ts), root + apps/mobile + web typecheck clean (the @gm/core/format alias resolves in mobile via tsconfig path + in web via package.json exports), prod build exit 0 no missing-export. All 3 merged through required CI (verify+e2e+mobile+lint+self-validation+migrations). Baseline green at start (validation 8/8 READY 0 unmet, prices byte-consistent, SUMMARY base $33,450/floor_met false, 0 open PRs, clean tree). run 92 — 0 gate failures across all 4 clears. Baseline green at start (typecheck 6/6, 1060 core tests +27 skip, prod web build exit 0 no missing-export, validation 8/8 READY 0 unmet, 0 open PRs, clean tree). #638 — 1 self-caught typecheck miss on the NEW test file (noUncheckedIndexedAccess flagged `const [item] = …[0]` as possibly-undefined; fixed with a `one()` helper that throws if absent, BEFORE commit — not a gate failure), then full gate green (1060 core tests incl. mobile-view.test.ts, typecheck 6/6, prod build exit 0, mobile tsc). #637 mobile-only (mobile tsc green). #639 core typecheck + persist tests (28) + prod build green. #640 className-only (eslint --max-warnings=0 + prod build green). All 4 merged through required CI (verify+e2e+mobile+lint+self-validation+migrations). run 91 — 0 gate failures: #634 (nutrition) — full @gm/core suite green (1052 pass +27 skip), typecheck 6/6, production web build exit 0 no missing-export; the 1 review cycle was a FRAMING correction (Reviewer B), not a gate/code failure. Baseline green at start (typecheck 6/6, 1050→1052 core tests, prod build exit 0 no missing-export, validation 8/8 READY 0 unmet, 0 open PRs, clean tree). run 89 — 0 gate failures: both a11y PRs (#630/#631) are className-only; apps/web typecheck + production build green (no missing-export) on each; #630's rework (btn-sm→base .btn) re-verified green; #631's initial JSX-comment placement was caught by typecheck and fixed before push. run 88 — 0 gate failures: #625 docs-only (no build impact); #626 core suite green (1050 pass +27 skip) + typecheck; #627/#628 web typecheck green + min-h-[44px] already a JIT class; all 4 merged through required CI (verify+e2e+mobile+lint+self-validation). Baseline green at start (typecheck 6/6, 1049 core tests +27 skipped, prod build exit 0 no missing-export, validation 8/8 READY 0 unmet, 0 open PRs, clean tree). run 84 — no code changes to verify (quiet convergent run); tree clean, branch level with origin/main, validation 8/8 --readiness READY 0 unmet. run 83 — baseline green at run start (branch level with main, tree clean, validation 8/8 --readiness READY 0 unmet). Per-change verify: #605 mobile typecheck green (`npm ci` then `tsc --noEmit`); #606 docs-only (no build impact); #607 full `@gm/core` suite green (1043 passed, thresholds met) + core typecheck clean. All 3 merged through required CI (verify + e2e journeys + mobile + lint + self-validation + migrations) — green. Housekeeping branch cut from latest origin/main.
    review_rejections: 3         # run 93 — 3 REQUEST_CHANGES, all CORRECT and converged/actioned (maker≠checker working as intended): (1) #643 v1 Reviewer B — the mobile-local titleCase copy is avoidable duplication since the native app CAN import @gm/core via the tsconfig alias → reworked to @gm/core/format; (2) #643 v2 Reviewer B — the "single source of truth" claim was false while apps/web/app/lib/format.ts kept its own copy → made web re-export from @gm/core/format; (3) #645 Reviewer A — the 44px layout-shift correctness point (→ abandoned, see abandoned_reasons). #642 + #644 first-pass 2/2. run 92 — 8/8 first-pass APPROVE, 0 REQUEST_CHANGES. Reviewer notes were all confirming/non-blocking: #638 both flagged the pre-existing raw-`item.name` (no titleCase) follow-up [titleCase unreachable from the workspace-excluded mobile app]; #639 Reviewer A's "await stall vs round-trip" precision note was folded pre-PR by the maker (not a review cycle). run 89 — 3 of 4 Sonnet reviews first-pass approve; 1 REQUEST_CHANGES: Reviewer B on #630 v1 (btn-sm+min-h-[44px] = the run-88 documented anti-pattern, a 44px box wrapping a 12px text-xs label) — a CORRECT premise, not stale; reworked to base .btn+min-h-[44px] (the #628 idiom) → both APPROVE on v2. #631 first-pass 2/2. run 88 — 7 of 8 Sonnet reviews first-pass approve; 1 REQUEST_CHANGES: Reviewer B on #628 v1 (btn-sm+min-h-[44px] glued = tall pill / tiny label, off-precedent vs .chip-tap/onboarding which scale padding+font WITH height). Addressed by dropping btn-sm for base .btn + min-h-[44px] → both reviewers APPROVE on v2. (The identical pattern in #627, already 2/2-approved + merged before that feedback landed, was left as-is [a11y-compliant, functional] with a design-polish follow-up noted.) run 83 — 6/6 Sonnet first-pass approve (2 reviewers × 3 changes). #605: A (filter valid PremiumFeature slugs, gmail_import verified unreachable in apps/mobile, header subtitle still honest), B (store-acceptance-in-isolation is the right call, Set not over-engineered, removing an un-redeemable perk is honest not conversion-dishonest). #606: A (all 7 var names match env.ts, RevenueCat webhook genuinely timing-safe, no secret committed, prices correct), B (real docs-contradicts-code operability gap, not churn). #607: A (hand-traced float math incl. 0.7*3→2.1, line 60 genuinely exercised, reachable), B (genuinely-uncovered reachable branch, 3 assertions non-redundant, survives the trim bar).
    review_cycles_used: 2        # run 93 — #643 used 2 rework cycles (mobile-local copy → @gm/core/format → also web re-export) then 2/2; #642 + #644 first-pass 2/2, 0 cycles; #645 abandoned after 1 cycle (not iterated — the fix needs browser verification). run 92 — all 4 changes first-pass 2/2, 0 maker fix cycles (the #639 comment-accuracy fix was a maker-owned pre-PR amend, not a review-driven cycle). run 89 — #630 used 1 fix cycle (btn-sm→base .btn), then 2/2; #631 first-pass 2/2, 0 fix cycles. run 88 — #625/#626/#627 first-pass 2/2; #628 used 1 fix cycle (btn-sm→base .btn+min-h) then 2/2. run 83 — all 3 changes 6/6 first-pass approve, 0 maker fix cycles.
    circuit_breaker_trips: 0
    findings_rejected: 5         # run 92 — rejected/deferred with evidence: (1) ingest.ts:99/155 `purchaseRows[0]!.id`/`lineRows[0]!.id` flagged "CRITICAL ledger corruption" = FALSE POSITIVE (Drizzle .returning() on a single-row insert yields exactly one row or throws → the `!` is safe, no partial-ledger state reachable); (2) CORS "missing Access-Control-Allow-Origin" = NOT a hole (omitting ACAO is the secure same-origin default; adding `*` would be the bug); (3) Stripe webhook non-UUID `metadata.userId` = LOW/already-safe (metadata stamped server-side; a bad value only makes the write throw → caught, logged, 200); (4) log-cook.ts sequential appendLedgerAndReproject loop / ingest.ts N+1 = DEFERRED (appends are order-dependent on the accumulated ledger, not safely parallelizable; ingest normalization N+1 is a large risky refactor on a non-latency-critical path, runs 38/41 precedent); (5) depletion zero-delta aging / embedding zero-vector / recipe-import multi-Recipe = by-design or negligible. Security/RLS/Track-G + monetization lenses NO actionable findings (multi-instance ceiling owner-gated already-tracked; reach-gated #190 re-confirmed). run 89 — rejected/deferred with evidence: (1) correctness parse-receipt HTTP-200-with-error on zero items — FALSE POSITIVE (sole client demo-client.tsx:105 guards !res.ok||!data.items → no crash; 200-for-zero-items is a defensible distinction from the 502 error path; a 422 change is zero-behavior on the only caller = sub-bar); (2) design staples/page.tsx Set — real but DEFERRED again (needs the adjacent ~28px number input raised too + a browser visual check the headless env can't do; splitting the surface toggle-now/Set-later would be incoherent). Security + monetization lenses NO findings (reach-gated re-confirmed). run 88 — rejected/deferred with evidence: (1) Track-F expiring.ts:51 domain-null branch — already 100% covered (the {now}-only call covers the no-domain path, soap[household] covers the filter); (2) Track-F log-cook.ts:129 heuristic guard — DB-bound (behind db.select().innerJoin), skipped per run-78 lesson; (3) Track-F user-model.ts:113 default:break — impossible-case guard (unknown topic kind unreachable through the typed union); (4) design homepage nav btn-sm (page.tsx:299/308/315) — app-wide compact-header idiom; changing only home breaks header consistency, and text header links at ~28px are a deliberate pattern (nav mis-tap is cheap) → left; (5) design staples/page.tsx:169 Set — real but DEFERRED (needs the adjacent number input raised too for balance + a browser visual check → future .chip-tap polish pass, noted). Security + correctness scouts NO findings. run 84 — quiet convergent run; both adversarial scouts returned "nothing clears the bar" / "no buildable non-reach lever" (no findings to reject or defer). run 83 — 1 deep-audit finding REJECTED (design a11y: recipe-image alt=""→alt={title} across 9 pages is an anti-pattern — title is adjacent/overlaid text everywhere, so empty alt is the correct decorative-redundant choice; the recurring alt='' Haiku trap). 4 coverage candidates DEFERRED as marginal/redundant (reconcile confidence-dedup already covered on the confirmations branch; onboarding cap; semantic-layer degrade; ingest idempotency). 2 lenses CLEAN (security/RLS/Track-G, correctness/functional). Monetization RE-CONFIRMED reach-gated (base ≈ $33K < $100K = owner-GTM #190). QUALITY_SCORECARD ship gate stays MET (A). NOTE: #605 was NOT a clean win — it reversed run 80's deliberate gmail_import deferral (kept on the merits, but the process slipped; see recurring_failures).
  rolling_7d:
    merged_prs: 178              # run-93 #642 (mobile pantry names titleCase) + #644 (recipes Remix/Add-missing 44px a11y) + #643 (extract @gm/core/format, unify web+mobile, title-case list/use-it-up/spend) + run-93 bookkeeping (#645 abandoned, not merged); run-92 #638 (mobile pantry contract — CRITICAL dead-on-arrival) + #637 (mobile onboarding-finish res.ok guard) + #639 (reprojectStock parallel reads perf) + #640 (/ask starter-chips 44px a11y) + run-92 bookkeeping; run-91 #634 (nutrition fractional-servings macros) + run-91 bookkeeping #635; run-89 #630 (cook-loop 44px a11y) + #631 (first-run/conversion CTA 44px a11y) + run-89 bookkeeping; run-88 #625 (marketing dark-household honesty across 3 staged artifacts) + #626 (units MASS→VOLUME coverage) + #627 (pantry resolve-chip 44px) + #628 (review-queue bulk-action 44px) + run-88 bookkeeping; (runs 85-87 + gtm interleaved, not each re-tallied here). run-84 quiet convergent run (0 code PRs; bookkeeping-only). + run-83 #605 (mobile paywall gmail_import store-honesty filter) + #606 (billing env-var docs) + #607 (cook decimal-scale coverage) + run-83 bookkeeping + run-82 #602 (manage-subscription dark-household + Family-tier store-honesty gate) + #601 (install-prompt 44px dismiss a11y) + run-82 bookkeeping + run-81 #596 (help dark-household store-honesty) + #597 (reorder verdict-copy coverage) + #598 (effort no-instructions coverage) + run-81 bookkeeping + run-80 #593 (buildExtractionPrompt no-hint coverage) + #594 (mobile paywall dark-Household filter) + run-80 bookkeeping #595 + run-79 #591 (mobile AI-remix parity FLAGSHIP) + #589 (effort med-tier coverage) + #590 (units BFS dead-end coverage) + run-79 bookkeeping + run-77 #576 (lift.ts loss-verdict coverage) + #577 (user-model dislike-branch coverage) + #578 (ASO premium-features drift) + #579 (spend null-price coverage) + run-77 bookkeeping + run-76 #573 (humanize customer-facing copy) + #574 (OPERATIONS Support-URL consistency) + run-76 bookkeeping + run-75 #567 (ASO Support-URL) + bookkeeping + run-74 #565 (predict recommendQty branch coverage) + run-74 bookkeeping + run-73 bookkeeping (quiet converged run, 0 code changes) + run-72 #560 (web-push env-var doc fix) + bookkeeping + run-71 #555 (§44 Layer A prod-smoke) + bookkeeping + run-70 #553 + bookkeeping #552; run-69 #547/#548 (+ bookkeeping) + run-68 #543/#544 (+ bookkeeping) + run-67 #535/#536 + run-66 bookkeeping #533/#534; incl run-66 #531/#532, run-65 bookkeeping #528 + GTM #529/#530; many are FACTORY/GTM/growth meta-commits
    reverts: 0
    readiness_attempts: 0
    readiness_rejected: 0
    recurring_failures:          # short bullets: the SAME wall hit across ≥2 runs (→ harness proposal if it persists)
      - "STALE local origin/main (run 44): the env cloned + checked out the run-43 tip (d48c0dd/#385) as a DETACHED HEAD, but local `origin/main`/`main` refs still pointed at #369 (14 commits behind). Branching from `main` cut the feature branch off stale #369, which made #379's merged worker-stub fix APPEAR reverted (the pre-#379 `stub()` was on the stale base, not real main). RESOLVED: `git fetch origin main` fast-forwarded origin/main to #385, confirmed #379 IS merged (no regression), then `git branch -f main origin/main` + rebased the feature branch onto real main (clean — file-disjoint). LESSON: ALWAYS `git fetch origin main && git branch -f main origin/main` (or branch from `origin/main`, not `main`) at run start BEFORE trusting local main / diagnosing a 'missing fix' — a stale ref manufactures phantom regressions. RECURRED run 53: local `main` was 36 commits behind origin/main; branches cut from it carried phantom diffs (#451's wrapped `empty` reformatting appeared as mine). Same fix — `git reset --hard origin/main` then recreate branches from stash (only wrapped.tsx had changed upstream; the stash already contained #451, so per-file checkout = origin/main + my edit). 3rd occurrence (runs 44, 53, 56). Run 56 exposed a GAP in the standing mitigation: the run-start `git reset --hard origin/main` ran on a DETACHED HEAD, so it moved HEAD but NOT the local `main` REF — then `git checkout -b <branch> main` still cut from stale `main` (missing run-55's `/demo` middleware, looked like a regression). Caught immediately via `git show origin/main` + fixed by `git branch -f main origin/main` and recreating the branch from `origin/main`. BENIGN (no bad merge). Standing mitigation UPGRADED: branch explicitly from `origin/main` (`git checkout -B <branch> origin/main`) — never from local `main` — and `git branch -f main origin/main` at run start. 3rd occurrence → harness proposal opened (`loop: harness improvement proposal`) to make the run-start sync detached-HEAD-safe."
      - "branch-entanglement (runs 39, 41, 61, 68): a checkout moved HEAD so work landed on the wrong branch. Run 68 was a NEW shape (self-inflicted, not a subagent): the §11 files were created + committed while HEAD was still on the docs branch (claude/docs-artifact-accuracy), so the media commit STACKED on the docs commit LOCALLY — but the REMOTES stayed clean (docs-artifact-accuracy remote only ever had the docs commit; PR #543 unaffected). Caught BEFORE opening the media PR by the run-61 pre-arm guard: `git rev-list --count origin/main..<branch>` returned 0 (empty branch) + a 422 'No commits between main and <branch>' on PR-create. Fixed cleanly: `git checkout -B claude/media-launch-briefs origin/main && git cherry-pick <media-commit>` (single commit, only the 4 media files — `git show --stat` confirmed disjoint from docs), then `git branch -f claude/docs-artifact-accuracy origin/<docs-branch>` to drop the stray local commit. No lost work, no bad merge. MITIGATION ADDED: after every `git checkout -b <branch>`, verify `git branch --show-current` matches before creating files (the switch silently didn't take this run) — plus the standing run-61 pre-arm checks (`git rev-list --count origin/main..<branch> > 0` + disjoint `git diff --stat`). Prior shapes below. --- Runs 39/41 were HARMLESS (tree MIX on top of a correct pushed commit; recover via `git reset --hard HEAD`). Run 61 was WORSE — the checkout moved HEAD during a `git checkout -b`, so a freshly-cut branch (`claude/experiment-lift-edge-tests`) was accidentally based on ANOTHER change's commit instead of main → the pushed branch bundled 5 files (NOT disjoint), which would have violated the disjoint rule at merge. Caught BEFORE arming auto-merge by `git diff --stat origin/main origin/<branch>` (the run-41 lesson: trust the pushed ref, not the shared tree) + both reviewers flagging the stacked-branch artifact. Fixed cleanly: `git checkout -B <branch> origin/main && git cherry-pick <single-file-test-commit>` + `--force-with-lease`. No lost work, no red merge. MITIGATION ESCALATED: (a) prefer worktree isolation for parallel agents that may checkout; (b) MANDATORY pre-arm checks on EVERY freshly-cut branch — `git log --oneline -2 origin/<branch>` (confirm parent is main) + `git diff --stat origin/main origin/<branch>` (confirm disjoint file set) — before enabling auto-merge. 3rd occurrence in 3 different failure shapes; if it recurs, open a harness proposal for worktree-per-subagent."
      - "recurring #0a6e33 re-flag (runs 35, 41): a design scout keeps proposing `brand-solid` for the deep-green-on-white hardcode (an AA regression). RESOLVED in #372 by moving to the byte-exact `brand-solid-hover` token + an in-code contrast comment at each site — the re-flag can't recur. No harness proposal needed."
      - "admin low-value design re-flag (runs 48, 49, 50): a design scout keeps surfacing internal-admin design nits — run 48/49 the `text-brand-solid`→`-hover` contrast INVERSE trap (`brand-solid`/`-hover` are SURFACE tokens, correct for `bg-*` where hover=darker, but as `text-*` FOREGROUND on the near-black admin bg darker = LESS contrast, a dark-mode regression, why #424 was abandoned); run 50 the admin/growth + admin/waitlist section-title `<p>` semantics (WCAG 1.3.1) + raw `text-amber-600`/`text-green-600` tokens. All admin is owner-internal, never store-reviewed, low-surface — deferred each time (run-45 precedent) to a dedicated admin design/dark-mode pass, never a per-element hack. Watch: if admin design re-flags again, either do the one dedicated admin pass (mode-aware foreground token + `<h2>` section titles + semantic status colors) OR add in-code comments at each admin site (like #372 did for the light-mode case) to stop the recurring re-flag."
      - "re-litigation of a deliberate deferral (run 83): the perf/mobile scout re-surfaced `gmail_import` on the mobile paywall as a 'new' store-honesty bug, and the maker shipped #605 (filter it out) as a fresh oversight WITHOUT cross-checking the ledger for the SPECIFIC prior decision — run 80 had DELIBERATELY deferred filtering it (reasoning: it's a live cross-platform feature whose value reaches a mobile user via the shared account) and flagged it 'a considered judgment call, not an oversight' for future runs; run 82 re-affirmed. PR #605 auto-merged before the run-80 note was found. OUTCOME kept on the merits (store-acceptance-in-isolation is the binding launch gate; a reviewer has no in-app path to Gmail import → a real Apple 2.3.1 vector; dropping 1/6 perks is marginal; run 80 named 'filter' as a legit option) — so the decision is now settled the OTHER way, WITH engagement. But the PROCESS slipped: the maker checked the household/Family *pattern* precedent, not the `gmail_import` *decision*. LESSON: a scout finding on a known-deferrable surface MUST be cross-checked against the ledger's SPECIFIC prior decision BEFORE arming auto-merge; and a scout's 'already-settled' brief should enumerate the specific deferred items (gmail_import), not just the pattern class. 1st occurrence — watch for recurrence; if a deliberate deferral gets flipped again without ledger cross-check, open a harness proposal to add a mandatory pre-arm ledger-grep gate."
      - "Haiku-scout false positives keep needing re-verification (runs 42,43,44,45,47): a cheap scout produces a plausible 'bug' against a deliberate design (ewmaConsumptionRate; server-action raw throws; recipe alt='') or an environment artifact (mobile '771 TS errors' = deps-not-installed). MITIGATION already in the loop: the orchestrator VERIFIES every scout finding before selecting (design intent at the site; `npm ci` before a mobile typecheck; adjacent-text check before an alt='' 'fix'). Working as intended — the verify step catches them; NOT a harness proposal (the model split expects cheap scouts to over-report; the maker's verification is the guard)."
    harness_proposals_open: 1    # run 56: #476 — make the run-start local-main sync detached-HEAD-safe (stale-main trap 3rd occurrence, runs 44/53/56). (#232 resolved by #234.)
  signal: steady                 # bootstrapping | improving | steady | churning | stuck
                                 #   run 93: PRODUCTIVE CONVERGENT run — 3 file-disjoint clears shipped (#642/#644/#643), 1 abandoned
                                 #   (#645, judgment abandon on an unverifiable layout tweak). Deep audit not due (run 92 ran a full 7-lens
                                 #   sweep <24h prior); a focused 3-Haiku scout advanced the run-92 follow-ups. The 3 REQUEST_CHANGES this
                                 #   run are a HEALTHY signal, not churn: each was correct and the change converged strictly better (mobile-local
                                 #   copy → shared @gm/core/format → web+mobile unified through one implementation — a genuine DRY win removing two
                                 #   duplicate copies), and the one abandon was the disciplined call (don't ship an unverifiable layout change on a
                                 #   design-bar repo). Value-bar-clearing polish: completed the mobile "never show raw slugs" story (4 screens now
                                 #   match web) + 2 cook-loop touch-target clears. Convergence unchanged: reach-gated (base ≈$33K < $100K = owner-GTM
                                 #   #190, no buildable non-reach lever, ~11th consecutive run), 'ready' NOT opened, Confidence UNCHECKED,
                                 #   validation 8/8, ship gate MET (A). Steady (holding correctly at the owner-GTM boundary; not stuck, not churning).
                                 #   run 84: QUIET CONVERGENT run (0 code changes, 0 abandons/reverts). Deep audit not due (run 83 ran one
                                 #   same day). Two lean adversarial Haiku scouts over the identical commit both returned no bar-clearing work:
                                 #   (a) no genuinely-new value-bar-clearing change beyond run 83's already-deferred marginal items / A+ purist
                                 #   nits; (b) NO buildable non-reach revenue lever — every pricing/tier/conversion/retention/margin lever is built,
                                 #   the $67K floor gap is demand-driven (~2.7-3× downloads = owner GTM #190). Correctly shipped NOTHING (padding is
                                 #   FORBIDDEN); a quiet coherent run is a SUCCESS. Convergence unchanged: reach-gated, 'ready' NOT opened, Confidence
                                 #   UNCHECKED, validation 8/8, ship gate MET (A). Steady (not stuck: the loop is correctly holding at the owner-GTM
                                 #   boundary, not failing to converge — everything within the loop's control is done).
                                 #   run 83: standalone 6-Haiku DEEP AUDIT (due since run 77). 2 lenses CLEAN (security/RLS/Track-G,
                                 #   correctness/functional). 3 file-disjoint clears, 6/6 first-pass Sonnet approve, 0 abandons: #605
                                 #   (mobile paywall gmail_import store-honesty filter — a REVERSAL of run 80's deliberate deferral, kept
                                 #   on the merits [store-acceptance-in-isolation]; process slipped, see recurring_failures) + #606 (billing
                                 #   env-var docs, living-artifact) + #607 (cook decimal-scale coverage, Track F). 1 finding rejected (alt=''
                                 #   anti-pattern) + 4 coverage candidates deferred (marginal/redundant). Convergence unchanged: monetization
                                 #   reach-gated (base ≈ $33K < $100K, owner-GTM #190, no buildable floor-mover); did NOT open 'ready';
                                 #   Confidence stays UNCHECKED; validation 8/8, 0 unmet; ship gate MET (A). Steady (not churning: 0
                                 #   abandons/reverts, 3 real clears; the #605 process slip is logged, not a reverted change).
                                 #   run 82: 5-Haiku scout sweep (folded deep-audit-equivalent: Track-F coverage · security/RLS/Track-G ·
                                 #   design/a11y+living-artifact · correctness/mobile-parity · monetization/store-honesty). 3 of 5 lenses
                                 #   NO-FINDINGS (security/RLS/Track-G, Track-F coverage [no clean gap after sibling+barrel verification],
                                 #   correctness/mobile-parity). 2 file-disjoint clears, 4/4 first-pass Sonnet approve, 0 abandons, 0 rejections:
                                 #   #602 (manage-subscription was the LAST un-gated surface advertising the dark FEATURE_HOUSEHOLDS feature —
                                 #   "household sharing" CTA copy + an unconditional Family plan card — gated both behind householdsEnabled() to
                                 #   match /upgrade/help/landing/mobile; Apple 2.3.1) + #601 (install-prompt PWA dismiss button 28px→44px, the
                                 #   #584 banner-dismiss idiom in a second component). Convergence unchanged: monetization reach-gated (base ≈ $33K
                                 #   < $100K, owner-GTM #190, no buildable floor-mover); did NOT open 'ready'; Confidence stays UNCHECKED;
                                 #   validation 8/8, 0 unmet; ship gate MET (A).
                                 #   run 81: 4-Haiku scout sweep (folded deep-audit-equivalent). Security/RLS/Track-G lens NO-FINDINGS.
                                 #   3 file-disjoint clears, 6/6 first-pass Sonnet approve, 0 abandons: #596 (help page was the lone
                                 #   surface still documenting the dark FEATURE_HOUSEHOLDS feature as live — gated it behind householdsEnabled()
                                 #   to match page.tsx/upgrade; Apple 2.3.1 / LIVING-ARTIFACTS) + #597 (first `reason`-copy coverage on the
                                 #   home reorder verdict — plural/singular branches) + #598 (estimateEffort no-instructions path). 1 finding
                                 #   rejected as already-addressed (getAdminDb DIRECT_DATABASE_URL fallback already logs loud + is a deliberate
                                 #   dev/CI carve-out). Convergence unchanged: monetization reach-gated (base ≈ $33K < $100K, owner-GTM #190, no
                                 #   buildable floor-mover); did NOT open 'ready'; Confidence stays UNCHECKED; validation 8/8, 0 unmet; ship gate MET.
                                 #   run 79: 6-Haiku scout sweep (deep audit NOT due — run 77 folded one <24h earlier). 4 of 6 lenses
                                 #   NO-FINDINGS (security/RLS/Track-G, design/a11y/taste, living-artifact/pricing, correctness/dead-code). 3
                                 #   file-disjoint clears: #591 (FLAGSHIP — mobile AI-recipe-remix PARITY: the mobile paywall advertised
                                 #   "recipe remix" but the native app had no way to REACH it vs the web /remix/[id] page; built a server-side-
                                 #   gated backend route + native screen + cook entry + icon, keyless-first + degrade) + #589 (effort "med"
                                 #   tier coverage) + #590 (units BFS dead-end null coverage). 6/6 Sonnet approve; #591 took 1 review cycle —
                                 #   Reviewer A caught 2 REAL bugs (premium gated on if(billingOn) → free-quota downgrade; no staleness guard on
                                 #   axis-switch fetch), both fixed + re-approved. 2 candidates rejected (mobile Family card = dark-feature trap;
                                 #   consume.ts empty-token guard = weak/defensive). LESSON: "advertised-but-missing" premium perks on the
                                 #   SECONDARY (mobile) platform are a real high-value gap class — cross-check every paywall claim vs a reachable
                                 #   mobile surface. Convergence unchanged: monetization reach-gated (base ≈ $33K < $100K, owner-GTM #190, no
                                 #   buildable floor-mover); did NOT open 'ready'; Confidence stays UNCHECKED; validation 8/8, 0 unmet; ship gate MET.
                                 #   run 76: QUIET CONVERGED run — deep audit NOT due (runs 74+75 ran/within same day, <24h) → straight to a
                                 #   full 5-Haiku scout sweep. 3 lenses NO-FINDINGS (correctness/functional, security/RLS/Track-G, and coverage/
                                 #   mobile after verification). 2 file-disjoint clears, both 2/2 first-pass approve, 0 abandons: #573 (design —
                                 #   humanized dev-jargon DB-error copy on /list,/pantry,/review + /list Amazon ASIN/"vertical" jargon; the 3
                                 #   error pages were outliers vs 7+ already-plain pages) + #574 (artifact — OPERATIONS.md go-live Support-URL
                                 #   /help→/support, the lone straggler of the drift #567 fixed). 3 coverage candidates VERIFIED padding/unreachable
                                 #   + REJECTED (tauDays<=0 always-30, analyze names.get ?? "" always-populated, default-param fallbacks). LESSON:
                                 #   after a canonical-value fix, grep the WHOLE repo for the OLD value — #567+#574 were the same /help drift found
                                 #   in two passes. Convergence unchanged: monetization reach-gated (base ≈ $33K < $100K, owner-GTM #190, no
                                 #   buildable floor-mover); did NOT open 'ready'; Confidence stays UNCHECKED; validation 8/8, 0 unmet; ship gate MET.
                                 #   run 73: QUIET CONVERGED run — deep audit NOT due (run 72 ran one same-day, <24h), went straight to a
                                 #   4-lens Haiku scout sweep (design/a11y/taste · test/eval coverage · correctness/dead-path · artifact-
                                 #   freshness). 2 lenses NO-FINDINGS; 3 candidates VERIFIED sub-bar + REJECTED (2x aria-hidden-on-decorative-
                                 #   icon = cherry-picking an inconsistently-applied convention across 40+ sites, marginal no-op SR effect;
                                 #   grocerymanager.app store-doc 'domain' = the CANONICAL brand domain used identically in code + docs, not
                                 #   drift). NOTHING cleared the value bar → 0 code changes, single bookkeeping PR. 0 abandons, 0 circuit breaks.
                                 #   Convergence unchanged: monetization reach-gated (base ≈ $33K < $100K, owner-GTM #190); did NOT open 'ready';
                                 #   Confidence stays UNCHECKED; validation 8/8, 0 unmet. A quiet coherent run = SUCCESS (value bar is the limiter).
                                 #   run 72: folded 5-Haiku deep-audit sweep (~24h since run-70) — ALL 5 lenses CLEAN except 1 real
                                 #   artifact-drift shipped (#560 web-push env var doc fix, both reviewers first-pass APPROVE) + the standing
                                 #   middleware perf gap (deferred: load-bearing auth refactor, wrong blast radius unattended; #320 non-ship-
                                 #   critical at its ≥B target). 3 scout candidates VERIFIED false/sub-bar + REJECTED (CORS-ACAO would weaken
                                 #   security = run-35 regression; inline-→-text-link = established convention run-59; consume.ts usedUnmeasured
                                 #   count NEVER surfaced = churn). 0 abandons, 0 circuit breaks. Convergence unchanged: monetization reach-gated
                                 #   (base ≈ $33K < $100K, owner-GTM #190, no buildable floor-mover); did NOT open 'ready'; Confidence stays
                                 #   UNCHECKED; validation 8/8, 0 unmet. QUALITY_SCORECARD ship gate stays MET (A, run 70).
                                 #   run 69: no standalone deep audit (run 66 3 runs ago; folded into a 5-lens proportionate scout sweep). 2
                                 #   file-disjoint mobile clears advancing the design_taste ship-critical dimension, both first-pass 2x-APPROVE,
                                 #   0 abandons. #547 (a11y — 10 apps/mobile screens' Pressable/Link CTAs got accessibilityRole+label, matching
                                 #   index.tsx's convention; Reviewer A traced expo-router Link runtime prop-forwarding). #548 (design — paywall's
                                 #   last raw ★ glyph → Ionicons Star registry icon). KEY FINDING: the design_taste=B scorecard that broke the ship
                                 #   gate (docs/quality, 2026-07-11) is STALE — its gap (mobile no icon system + web ▾) was closed by runs 64/65 +
                                 #   the web ChevronDown, and this run closes the residual glyphs; Quality Auditor (maker≠checker) owns re-grading to
                                 #   A. Rejected: correctness scout embed()-not-metered (risky core-path MeterContext thread for telemetry-only, no
                                 #   pre-launch data — ingest-N+1 verdict); security = all owner-config in PENDING_OPS; monetization reach-gated (#190).
                                 #   Convergence unchanged: did NOT open 'ready' (Confidence stays UNCHECKED); validation 8/8, 0 unmet.
                                 #   -- prior run 68: no deep audit (run 66 <24h). Proportionate 5-Haiku scout sweep → 2 file-disjoint clears, both
                                 #   first-pass 2x-APPROVE, 0 abandons. #543 (LIVING-ARTIFACTS — docs-vs-code sweep: OPERATIONS.md had the
                                 #   AUTH_SECRET/NEXTAUTH_SECRET roles SWAPPED (operator-harming), .env.example missing the required
                                 #   NEXTAUTH_SECRET, README phantom services/amazon-mcp → real apps/mobile). #544 (FLAGSHIP, §11 lowest track —
                                 #   the launch-brief AUTHORING consumer the ROADMAP named: LAUNCH_CREATIVE_BRIEFS grounded in the real
                                 #   LAUNCH_PLAN/BRAND_KIT + auditLaunchBriefs + stageLaunchCreative batch driver; real assets stay owner-gated so
                                 #   §11 box stays [ ]). Security + design scout findings correctly REJECTED (MARGIN_INGEST_URL attacker-env
                                 #   non-boundary; cook-mode aria-label conveys real SR info). Convergence stays reach-gated (#190, base ≈ $33K <
                                 #   $100K, owner-GTM); did NOT open 'ready'; Confidence stays UNCHECKED. Validation 8/8, 0 unmet. A branch-
                                 #   entanglement snag (media commit stacked on the docs branch locally) was caught pre-PR + fixed clean (4th
                                 #   occurrence, new shape; remotes never polluted).
                                 #   -- prior run 66: DEEP AUDIT (5-Haiku lens, due since run 63 >24h) + 2 file-disjoint clears. #532 (FLAGSHIP,
                                 #   security/reliability — the signup check-then-insert is not atomic → a concurrent same-username race
                                 #   loses to the users.username UNIQUE constraint (SQLSTATE 23505) → previously an UNHANDLED 500 on the
                                 #   funnel-entry path; try/catch → EXISTING /signup?error=exists + a pure isUniqueViolation() classifier in
                                 #   new @gm/core/security/pg-error + 6 keyless tests; Reviewer A verified vs the real postgres/drizzle-orm
                                 #   source). #531 (store-review artifact drift — ACCEPTANCE_AUDIT.md paywall path → real apps/mobile/app/
                                 #   upgrade.tsx). 6 reviews all APPROVE first-pass; 0 abandons. Deep audit: 3 of 5 lenses CLEAN, 2 findings
                                 #   shipped, false positives correctly rejected (recipe alt='' decorative-WCAG; loadSavedRecipes cap =
                                 #   speculative churn). Convergence stays reach-gated (#190, base ≈ $33K < $100K, owner-GTM); did NOT open
                                 #   'ready'; Confidence stays UNCHECKED. Scorecard design_taste still B (run-64 icon fix is the Quality
                                 #   Auditor's to re-grade — maker≠checker; this run's deep audit independently found design CLEAN but the loop
                                 #   does NOT self-grade). Validation 8/8, 0 unmet.
                                 #   -- prior run 65: 6-Haiku scout sweep (deep audit NOT due, run 63 <24h) + 3 file-disjoint clears. #527 (FLAGSHIP —
                                 #   a §28 silent-green on the MONETIZED Gmail-import path: missing TOKEN_ENC_KEY silently stored null OAuth
                                 #   tokens → Connect-Gmail "succeeds" then sync fails "no valid google token"; canonical isProdRuntime
                                 #   classifier @gm/core/security/token-enc-guard + 7 keyless tests + fail-open-but-loud prod log; storage gated
                                 #   on willEncrypt so a blank key degrades to null, not a throw). #525 (last unlabeled mobile input →
                                 #   accessibilityLabel, WCAG 2.1 A). #526 (real untested lift.ts branch — control-sufficient + no-challenger-
                                 #   data → leading_variant null; reviewer mutation-tested non-vacuous). 8 reviews (5 first-pass APPROVE + 1
                                 #   REQUEST_CHANGES on #527 fixed in cycle 2 → 2 APPROVE); 0 abandons. Monetization scout re-confirmed
                                 #   reach-gated (all remaining marginal levers ~$4–9K/yr < $67K gap); deferred trial-ending T3 email (padding).
                                 #   Convergence stays reach-gated (#190, base ≈ $33K < $100K, owner-GTM); did NOT open 'ready'; Confidence
                                 #   stays UNCHECKED. Scorecard design_taste still B (run-64 icon fix is the Quality Auditor's to re-grade —
                                 #   maker≠checker). Validation 8/8, 0 unmet. +set-token-enc-key owner action.
                                 #   -- prior run 64: SHIP-GATE UNBLOCKER — the QUALITY_SCORECARD (as_of 2026-07-11) blocked the ship gate SOLELY on
                                 #   design_taste=B: apps/mobile (App Store/Play scope per eas.json) had NO icon system, ~110 raw ←→›✓ glyphs
                                 #   as icons vs the web PWA's hand-built registry. 2 file-disjoint clears: #522 (FLAGSHIP — new
                                 #   apps/mobile/lib/icons.tsx over @expo/vector-icons Ionicons mirroring the web registry; converted the 5
                                 #   flagged surfaces — home cards, recipes ›, cook-mode ←/→/✓, onboarding+upgrade ✓) + #523 (web ▾ swap-
                                 #   disclosure caret → registered-but-unused ChevronDown). 3/4 reviews first-pass APPROVE; Reviewer B's 1
                                 #   REQUEST_CHANGES (Wrapped=sparkles → the web's deliberately-avoided AI-flourish glyph) fixed → trophy-outline
                                 #   + in-code rationale, then APPROVE. 0 abandons. No deep audit (run 63 ran one <24h prior). design_taste fix
                                 #   SHIPPED — grade is the Quality Auditor's to re-assess (maker≠checker). Convergence stays reach-gated (#190,
                                 #   base ≈ $33K < $100K, owner-GTM); did NOT open 'ready'; Confidence stays UNCHECKED. Validation 8/8, 0 unmet.
                                 #   -- prior run 63: DEEP AUDIT (4-Haiku lens, due since run 59) + 3 file-disjoint clears. #514 (the top audit
                                 #   finding — three growth admin routes' bare `await auth()` outside try/catch → 500 not 403; swapped to
                                 #   the non-throwing currentSession() helper) + #517 (same-pattern follow-on — LaunchGuard's
                                 #   forceSignOutAction had the same bare auth() → crashed EVERY launch; same currentSession() swap, signOut's
                                 #   NEXT_REDIRECT left to propagate) + #515 (Track-E/§11 batch STAGING consumer stageCreative on the run-62
                                 #   media adapter — CreativeBrief → metadata-only StagingManifest + raw results, degrade/never-throw
                                 #   preserved+extended with an exhaustive off-type default arm). 6/6 reviews APPROVE; #515 folded in
                                 #   Reviewer A's non-blocking off-type hardening pre-merge; 0 abandons. Deep audit: 3 of 4 lenses CLEAN
                                 #   (security/abuse+RLS, test/eval coverage, design+a11y+artifacts) — 1 real correctness finding, 2 sites
                                 #   (#514/#517). §11 box STILL NOT ticked (consumer built+keyless-proven; real-key production + a brief-
                                 #   authoring invocation site remain — EVIDENCE-BASED DONE). Convergence stays reach-gated (#190, base ≈ $33K
                                 #   < $100K, owner-GTM); did NOT open 'ready'; Confidence stays UNCHECKED. Validation 8/8, 0 unmet. PROCESS:
                                 #   #517 shipped after #516 bookkeeping merged mislabelling it a follow-up → same-run correction PR; sweep
                                 #   all same-pattern sites in the first batch next time.
                                 #   -- prior run 62: 4 changes — the FLAGSHIP is the LOWEST incomplete BUILDABLE track item (Track-E §11
                                 #   media-gen adapter #509/#442: @gm/core/media, image/video/music/voiceover on the existing Gemini
                                 #   key, degrade-by-default + audit-first maker≠checker pre-publish gate, STAGES+audits only) — first
                                 #   time this cadence a genuine unbuilt Track-E capability (not just Track-F/G polish) was available.
                                 #   +3 file-disjoint small clears (#510 privacy tier Pro→Premium / #511 README email→username signup /
                                 #   #512 6 mobile TextInput a11y labels). 3 small = 6/6 APPROVE first-pass; flagship = 2/2 after ONE fix
                                 #   cycle (reviewers caught a reused LLM_TIMEOUT_MS knob → dedicated MEDIA_TIMEOUT_MS; a false VISION
                                 #   avoid-list attribution → honest curated denylist; + injected-provider seam raised coverage 46→90.6%).
                                 #   0 abandons. Scout sweep: Track-F + security both CLEAN (converged). §11 box NOT ticked (adapter
                                 #   built; end-to-end staged-creative wiring is follow-up — EVIDENCE-BASED DONE). Deep audit NOT due
                                 #   (run 59 <24h). Convergence stays reach-gated (#190, base ≈ $33K < $100K, owner-GTM); did NOT open
                                 #   'ready'; Confidence stays UNCHECKED. Validation 8/8, 0 unmet. +marketing-media-gen capability.
                                 #   -- prior run 61: 3 file-disjoint clears — #504 FLAGSHIP spend-integrity (ask-quota throw-path settlement,
                                 #   closes the exact residual #482/run57 deferred: a mid-loop agent throw charged 1 vs up to maxSteps×
                                 #   already-billed Gemini calls; typed ChatToolLoopError carries the real count) + #505/#506 Track-F
                                 #   coverage of two money/conversion decision paths (isTrialEligible; computeExperimentResult edges,
                                 #   #470's gap). 0 abandons, 6 Sonnet reviews (2/PR) ALL APPROVE first-pass, 0 verify-cycle failures.
                                 #   5-Haiku scout sweep; design=churn/WCAG-correct-as-is, env-fail-loud=deliberate-degrade, artifact=
                                 #   no contradictions. Branch-entanglement trap recurred (3rd time, runs 39/41/61) — a non-disjoint
                                 #   branch caught pre-arm + fixed via cherry-pick onto origin/main. Deep audit NOT due (run 59 same-day).
                                 #   Convergence stays reach-gated (#190, base ≈ $33K < $100K, owner-GTM); did NOT open 'ready';
                                 #   Confidence stays UNCHECKED. Validation 7/7, 0 unmet.
                                 #   -- prior run 60: 1 file-disjoint clear (#495 mobile premium_family tier-label bug — /api/mobile/profile
                                 #   returns the full SubscriptionTier incl. premium_family, but profile.tsx typed tier with 3
                                 #   variants + no label → raw slug shown to Family subscribers; fixed + tightened TIER_LABEL to
                                 #   Record<tier,string> as a compile-time guard). 0 abandons, 2 Sonnet reviews APPROVE first-pass,
                                 #   0 verify-cycle failures. 4-Haiku scout sweep (design/security/monetization/mobile+artifacts);
                                 #   design=churn-only, security=owner-gated/marginal, monetization=no buildable lever (reach-gated
                                 #   re-confirmed). Deep audit NOT due (run 59 same-day). Convergence stays reach-gated (#190, base
                                 #   ≈ $33K < $100K, owner-GTM); did NOT open 'ready'; Confidence stays UNCHECKED. Validation 7/7.
                                 #   -- prior run 59: 3 file-disjoint clears (#491 design glyph→ChevronDown / #492 privacy-disclosure
                                 #   IAP correction / #493 billing scaffold-comment correction), 0 abandons, 6 Sonnet reviews
                                 #   (2/PR) ALL APPROVE first-pass, 0 verify-cycle failures, all required CI green first-pass.
                                 #   Full standalone 5-Haiku DEEP AUDIT (due since run 57): security/RLS+Track-G, correctness/
                                 #   functional, test-coverage+perf, design/a11y, artifacts/business-case — all CLEAN except the
                                 #   2 shipped design/artifact findings; #493 was a free second-audit find (Reviewer A noticed a
                                 #   stale comment while fact-checking #492's prices). Convergence stays reach-gated (#190, base
                                 #   ≈ $33K < $100K, owner-GTM; sweep found no buildable floor-mover); did NOT open 'ready';
                                 #   Confidence stays UNCHECKED. Validation 7/7, 0 unmet.
                                 #   -- prior run 56: 3 shipped (§34 Part B invite-code FLAGSHIP #475 + #474 mobile array-guard +
                                 #   #473 a11y triangle), 0 abandons, 6 Sonnet reviews (2/PR). Flagship took 2 review cycles:
                                 #   Reviewer A caught a REAL security defect (redeem granted the literal master
                                 #   SITE_GATE_PASSWORD to invitees) → fixed with a distinct SITE_GATE_INVITE_SECRET; cycle-2
                                 #   B caught dead getWaitlistInviteStats → wired into the issuance script; both re-APPROVE. CI
                                 #   caught 1 e2e locator strict-mode bug (getByLabel regex vs section aria-label) → fixed. The
                                 #   maker≠checker + real-browser e2e gates BOTH earned their keep. Stale-local-main trap recurred
                                 #   3rd time (detached-HEAD gap) → harness proposal opened. Convergence stays reach-gated (#190,
                                 #   base ≈ $33K < $100K, owner-GTM); did NOT open 'ready'; Confidence stays UNCHECKED. Validation 7/7.
                                 #   -- prior run 54: 4 file-disjoint clears, 0 abandons, 8 Sonnet reviews (2 per PR) ALL APPROVE
                                 #   first-pass (0 re-reviews). Standalone 6-Haiku DEEP AUDIT (due since run 53, ~2 days)
                                 #   covering all 8 areas: security/RLS+Track-G, correctness, design/a11y, monetization+
                                 #   business-case, mobile+perf, coverage+artifacts — 0 CRITICAL; security + coverage +
                                 #   artifacts CLEAN; monetization reach-gated RE-CONFIRMED (pricing MATCHES, no bugs; the
                                 #   annual-first/promo levers are A/B-experiment territory, not bankable — no buildable
                                 #   floor-mover). #464/#465 hardened the add-receipt+scan pre-LLM quota-gate DB read (uncaught
                                 #   throw → inline {status:error}, the #436/#437/#448 class on 2 core-loop vision paths).
                                 #   #466 a11y scan radio fieldset/legend (WCAG 1.3.1). #467 perf ask 7 sequential brief reads
                                 #   → Promise.all (#457 pattern, premium path). 3 scout false positives verified against real
                                 #   code + rejected (mobile onboarding res.ok no-op; upgrade aria-current is valid-in-a-set;
                                 #   aria-disabled redundancy churn) — the maker-verify guard earned its keep again. Convergence
                                 #   stays reach-gated (#190, base ≈ $33K < $100K, owner-GTM); did NOT open 'ready'; Confidence
                                 #   stays UNCHECKED. Validation 5/5, 0 unmet.
                                 #   -- prior run 52: 5 file-disjoint clears, 0 abandons, 10 Sonnet reviews (2 per PR): 9 APPROVE +
                                 #   1 REQUEST_CHANGES (honored — #449 drop redundant aria-label, keep aria-pressed). No deep
                                 #   audit (run 50 same day). 5-Haiku scout sweep; security + artifacts CLEAN. #447 push
                                 #   {ok}-contract + fake-"Notifications are on" fix; #448 import save DB-degrade; #449 cook
                                 #   aria-pressed; #450 spend week-branch test (+1, 872 total); #451 mobile Wrapped empty-state.
                                 #   Lesson: grep over raw subagent JSONL matched prompt-echo verdicts (3 phantom rejections);
                                 #   trust the completion-notification <result>. Convergence stays reach-gated (#190, base ≈ $33K
                                 #   < $100K, owner-GTM); did NOT open 'ready'; Confidence stays UNCHECKED. Validation 5/5, 0 unmet.
                                 #   -- prior run 50: 2 file-disjoint clears, 0 abandons, 4 Sonnet reviews (2 per PR) all APPROVE
                                 #   first-pass (0 re-reviews). Full 6-lens DEEP AUDIT (due since run 47, 3 runs): security/RLS+
                                 #   Track-G, correctness/dead-code, artifacts, perf/deps, design/a11y, mobile+functional — 5 CLEAN,
                                 #   1 design deferred (low-value internal-admin), 1 mobile cluster shipped. #429 completed the #427
                                 #   mobile/v1 route error-hardening sweep — 3 uncaught DB throws (recipes/[id], v1/pantry, v1/list)
                                 #   wrapped + serverError() (an uncaught HTML 500 to a JSON mobile client), + 6 bare catches given
                                 #   the missing G3 server-side log. #430 covered the untested UnitConverter item_base 2-hop BFS chain
                                 #   (4 exact-value tests; a dormant-but-real pure engine). Monetization/business-case re-confirmed
                                 #   reach-gated (no buildable floor-mover). Convergence stays reach-gated (#190, base ≈ $33K < $100K,
                                 #   owner-GTM); did NOT open 'ready'; Confidence stays UNCHECKED. Validation 5/5, 0 unmet.
                                 #   -- prior run 49: 2 file-disjoint clears, 0 abandons, 6 Sonnet reviews (4 first-pass + 2 re-review)
                                 #   all APPROVE. No deep audit (run 47 same day). 5-Haiku scout sweep. Security CLEAN (only
                                 #   known/owner items). #426 fixed a real mobile conversion dead-end — a discriminated-union
                                 #   API response cast to just its happy arm dropped the free-user paywall behind a cheerful
                                 #   "all caught up" empty state; typed the union + rendered the upgrade prompt. #427 hardened
                                 #   3 mobile routes (auth/profile/onboarding) that called the DB outside try/catch → uncaught
                                 #   500; wrapped to a controlled 503, and Reviewer B's should-fix added the missing G3
                                 #   server-side log (bare catch → catch(err)+console.error). Monetization reach-gated
                                 #   reconfirmed (Family-tier mobile "gap" is a RevenueCat OFFERING = owner config, not code).
                                 #   Convergence stays reach-gated (#190, base ≈ $33K < $100K, owner-GTM); did NOT open 'ready';
                                 #   Confidence stays UNCHECKED. Validation 5/5, 0 unmet.
                                 #   -- prior run 47: 3 file-disjoint clears, 0 abandons, 7 Sonnet reviews (incl. 1 re-review) all
                                 #   APPROVE. Full 6-lens DEEP AUDIT (due since run 45): security/RLS+Track-G, correctness,
                                 #   artifacts, monetization, design/a11y, mobile+perf+test — NO CRITICAL findings; security +
                                 #   correctness + artifacts CLEAN; monetization RE-CONFIRMED reach-gated (no buildable
                                 #   floor-mover). #418 completed the #372 brand-solid-on-white AA sweep (last 4 CTA outliers,
                                 #   4.45:1→6.4:1). #419 gave the previously-untested experiment-stats module 15 tests + a
                                 #   sign-bug MONOTONICITY guard (Reviewer A reimplemented + simulated the bug to confirm it's
                                 #   load-bearing). #420 closed a real mobile parity gap — the cook screen promised an "I cooked
                                 #   this" button that didn't exist; new POST /api/mobile/cook mirrors the proven web logCook
                                 #   path; Reviewer B's design catch (bottom placement + real servings) made it right. 2 scout
                                 #   false positives correctly rejected (recipe alt="" decorative-correct; mobile "771 errors" =
                                 #   deps-not-installed). Convergence stays reach-gated (#190, base ≈ $33K < $100K, owner-GTM);
                                 #   did NOT open 'ready'; Confidence stays UNCHECKED. Validation 5/5, 0 unmet.
                                 #   -- prior run 46: 3 file-disjoint clears, 0 abandons, all 6 reviewers APPROVE. Worked the
                                 #   open-issue backlog + a lean 3-Haiku scout sweep (no deep audit — run 45 <24h). #404
                                 #   closed the §32 signup-referral audit (#370) by making the never-throw contract
                                 #   testable (DI helper in @gm/core + 8 guard tests); #406 a11y file-input labels (WCAG
                                 #   3.3.2); #407 a §28 Stripe-webhook fail-loud on an unrecognized subscription price
                                 #   (mirrors the #380 captcha / #378 HMAC hardenings). The 2-reviewer gate earned its
                                 #   keep: #404-A's conditional block was resolved by the real source, #407-B surfaced an
                                 #   extra real bypass path. Convergence stays reach-gated (#190, base ≈ $33K < $100K,
                                 #   owner-GTM); did NOT open 'ready'; Confidence stays UNCHECKED. Validation 5/5, 0 unmet.
                                 #   -- prior run 45: converged quiet run + a full 5-lens DEEP AUDIT (due since run 41). 1 real
                                 #   value-bar clear (#390 a11y heading semantics on landing pricing + cook loop, WCAG
                                 #   1.3.1), 0 abandons, both reviewers 2/2 first pass. 2 scout findings correctly
                                 #   REJECTED: the ewmaConsumptionRate "inflation" bug (misreads a deliberate
                                 #   repurchase-cadence model) + the monetization "unbuilt levers" (all speculative/
                                 #   owner-dependent/scope-creep, stacked still below the $100K floor). Security/RLS/
                                 #   Track-G + artifacts re-confirmed CLEAN. Closed #359 (all 3 §28 fixes on main).
                                 #   Convergence stays reach-gated (#190); did NOT open 'ready'; Confidence stays UNCHECKED.
                                 #   -- prior run 44: converged quiet run — 1 real value-bar clear, 0 abandons, 2/2 first pass.
                                 #   #386 added the missing PRECISION/anti-hallucination half of the vision scan eval
                                 #   (it only measured recall; a phantom detection silently pollutes a real user's
                                 #   pantry — the exact failure detect.ts's presence+2D-box design targets), Track F /
                                 #   #319. Full 3-Haiku scout sweep (reliability, security/Track-G, design/a11y+mobile):
                                 #   3 candidates PASSED on judgment, not padding — CSP unsafe-inline/eval removal
                                 #   (needs a nonce migration + real-browser verify; hydration-break risk too high
                                 #   headless), mobile color centralization (18-file cosmetic churn + native BUILDS!=WORKS
                                 #   risk), and 3 server-action raw-throws (scout's "silent data loss/inconsistent state"
                                 #   framing was FALSE — app/error.tsx boundaries + withTenant transactions handle them
                                 #   gracefully; the inline-friendly-error pattern exists only for receipt parse because
                                 #   IT fails often). The run's load-bearing work was git hygiene: a STALE local origin/main
                                 #   (#369, 14 behind) manufactured a phantom "#379 worker-stub reverted" regression —
                                 #   fetch + rebase confirmed no regression. Security/RLS/Track-G re-confirmed CLEAN.
                                 #   Convergence stays reach-gated (#190); did NOT open 'ready'; Confidence stays UNCHECKED.
                                 #   -- prior run 41: converged run + a full 5-lens DEEP AUDIT (due since run 38). 2 real
                                 #   value-bar clears, 0 abandons, both 2/2 first pass. #371 corrected a stale README
                                 #   design-system bullet (dead Inter/Fraunces/aurora/bento/accent-themes/frosted-nav
                                 #   description → the shipped Hanken-Grotesk/single-accent/solid-nav reality; LIVING
                                 #   ARTIFACTS). #372 swapped the last 4 raw-hex #0a6e33 buttons → the byte-exact
                                 #   text-brand-solid-hover token + a WCAG-AA contrast comment at each site (100% TSX
                                 #   token discipline; PERMANENTLY ends the recurring "use brand-solid" mis-flag, which
                                 #   would regress contrast 6.38:1→4.45:1). Deep audit: Security/RLS/Track-G CLEAN (29
                                 #   tables), correctness/functional CLEAN, monetization RE-CONFIRMED reach-gated (the
                                 #   sole buildable lever — a collections add-on — is ~$1-3K/yr + collides with the locked
                                 #   subscription-only v1 decision), perf 3-of-3 rejected (ingest batching = <2% on an
                                 #   LLM-bound path + core-path regression risk), coverage clean. 0 reverts, 0 circuit
                                 #   breaks. The branch-entanglement trap recurred (run-39) but was harmless (pushed
                                 #   commits verified correct + disjoint; tree reset). Convergence stays reach-gated
                                 #   (#190); did NOT open the 'ready' issue; Confidence statement stays UNCHECKED.
                                 #   -- prior (run 35): converged quiet run — 2 real value-bar clears, 0 abandons. #315 raised the
                                 #   cook-mode timer + step-nav buttons to the 44px WCAG/Apple touch-target minimum
                                 #   (+timer aria-labels) on the app's most hands-busy surface; #316 fixed a real
                                 #   money-math bug in computeMrrUsd (per-sub round(3999/12) baked a 0.25¢/sub bias →
                                 #   $1 MRR understatement at 56 annual subs; now amortizes on the aggregate + regression
                                 #   test). The run's value was the FILTER: 5 of 7 scout candidates rejected on
                                 #   verification, TWO would have been active regressions (adding CORS ACAO:* = weaker
                                 #   security; swapping the deliberate #0a6e33 contrast hardcode to the AA-failing
                                 #   brand-solid token). Security/RLS/Track-G CLEAN; mobile IAP loop + vision invariant
                                 #   confirmed intact (2026-06-29 scorecard's ship-critical gaps remain closed). Monetization
                                 #   scout confirmed the honest floor is reach-gated (owner GTM), not lever-gated. 0 reverts,
                                 #   0 circuit breaks. Convergence stays reach-gated (#190) + grade-pending (scorecard STALE,
                                 #   independent re-grade owed). Did NOT open the 'ready' issue.
                                 #   -- prior (run 34): 4 real value-bar clears, 0 abandons — #308 mobile paywall off-brand
                                 #   purple→brand-green (the #1 conversion surface; design bar), #309 dropped the last
                                 #   deprecated grape/berry palette usages in apps/web for brand/danger tokens, #310
                                 #   completed the store premium-feature list (unlimited Discover + spend insights) to
                                 #   match billing/index.ts, #311 unit-covered the DB normalization ports keyless (a real
                                 #   skipIf-gated CI hole on the ingestion cascade). The 2-reviewer gate paid for itself
                                 #   TWICE without a single wasted change: Reviewer A caught a genuine contrast regression
                                 #   introduced by the color swap (#308) AND a false-confidence guard test via mutation
                                 #   testing (#311) — both fixed in a 2nd cycle, both then 2/2. 0 reverts, 0 circuit
                                 #   breaks, duplicate-coverage trap did NOT recur. Convergence stays reach-gated (#190,
                                 #   business-case floor) + grade-pending (the independent Quality Auditor must re-grade —
                                 #   the scorecard (2026-06-29) is STALE: its two named ship-critical B gaps were fixed in
                                 #   PRs #266/#263, and vision now shows ~100% coverage, not the ~0% it cites). Did NOT
                                 #   open the 'ready' issue.
```

## How to read it (owner)
- **`improving`/`steady`** — shipping clears the bar, few abandons/reverts, readiness attempts converge.
- **`churning`** — lots abandoned/reverted vs. shipped → the loop is busy, not better. Check
  `abandoned_reasons` for the pattern.
- **`stuck`** — `recurring_failures` keeps naming the same wall and `harness_proposals_open` is 0 → the META
  channel is dead; raise a `loop: harness improvement proposal`.
- A quiet honest run with a small shipped count + a clean signal is a SUCCESS; a high `changes_shipped` with
  a high abandon/revert rate is NOT.

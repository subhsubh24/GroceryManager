# dependencies
node_modules/
.pnpm-store/

# builds
dist/
.next/
out/
*.tsbuildinfo

# turbo
.turbo/

# env / secrets
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*

# os / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# test / coverage
coverage/

# local artifacts
/tmp/
*.local

# claude code agent worktrees (transient, harness-managed)
.claude/worktrees/

# local-only secrets / config (never commit)
.envl
.mcp.json

# locally-installed agent skills (not committed)
.agents/

# playwright scratch output (traces, error contexts, reports) — regenerable, never commit
test-results/
playwright-report/
# §44 live-prod smoke captures — transient health-probe artifacts (the owner-wired job re-captures each
# run against the live URL); the committed F6 artifacts live in __screenshots__/ (not this subdir).
apps/web/e2e/__screenshots__/prod-smoke/
